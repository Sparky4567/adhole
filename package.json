{
  "name": "adhole",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "gravity": "bun -e 'import { bootstrap } from \"./src/index\"; const s = await bootstrap(); await s.listManager.updateGravity(); process.exit(0);'"
  },
  "dependencies": {
    "dns-packet": "^5.6.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/dns-packet": "^5.6.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
